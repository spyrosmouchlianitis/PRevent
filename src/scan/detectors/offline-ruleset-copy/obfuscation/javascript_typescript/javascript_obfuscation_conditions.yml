rules:
  - id: javascript-obfuscation-conditions
    patterns:
      - pattern-not-inside: '...'
      - pattern-either:
          - pattern: if ($VAR) { ... }
          - pattern: while ($VAR) { ... }
          - pattern: switch ($VAR) { case ... }
      - metavariable-regex:
          metavariable: $VAR
          regex: ^(?!.*(?<!['"`])\b(?!true|false|undefined|null|NaN)[a-zA-Z_$]).*$
    message:  Detect obfuscation by spurious conditions.
    severity: WARNING
    languages: 
      - javascript
      - typescript
